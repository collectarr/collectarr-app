import 'package:flutter/material.dart';

class BlurayPage extends StatelessWidget {
  const BlurayPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Blu-ray')));
  }
}

