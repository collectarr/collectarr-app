enum PickListScope {
  media,
  release,
  ownedCopy,
  trackingLifecycle,
  customField,
  all,
}

enum PickListValueMode {
  single,
  multi,
}

enum PickListControlType {
  dropdown,
  checklist,
  tagList,
  personList,
  buttonBar,
}
