enum PickListScope {
  media,
  release,
  ownedCopy,
  trackingRecord,
  customField,
  all,
}

enum PickListValueMode {
  single,
  multi,
}

enum PickListControlType {
  dropdown,
  checklist,
  tagList,
  personList,
  buttonBar,
}
