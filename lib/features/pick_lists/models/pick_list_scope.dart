enum PickListScope {
  media,
  release,
  ownedCopy,
  trackingEntry,
  customField,
  all,
}

enum PickListValueMode {
  single,
  multi,
}

enum PickListControlType {
  dropdown,
  checklist,
  tagList,
  personList,
  buttonBar,
}
